buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Versão do Android Gradle Plugin
        classpath 'com.android.tools.build:gradle:8.2.1'  // Atualize para a versão mais recente, se necessário
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = "../build"
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
